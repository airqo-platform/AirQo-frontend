import React from 'react';

const CleanAirPress = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh'
      }}>
      Press
    </div>
  );
};

export default CleanAirPress;
