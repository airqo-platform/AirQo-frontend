import React from 'react';

const ForYou = () => {
  return (
    <div
      style={{
        overflow: 'hidden',
        position: 'relative',
        height: '100vh',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
      }}>
      For You
    </div>
  );
};

export default ForYou;
