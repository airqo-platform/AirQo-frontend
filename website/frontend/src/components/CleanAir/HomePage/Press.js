import React from 'react';

const Press = () => {
  return (
    <div
      style={{
        overflow: 'hidden',
        position: 'relative',
        height: '100vh',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
      }}>
      Press
    </div>
  );
};

export default Press;
