import 'package:flutter/material.dart';

class TestSearch extends StatelessWidget {
  const TestSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Text('Test Search'),
    );
  }
}
