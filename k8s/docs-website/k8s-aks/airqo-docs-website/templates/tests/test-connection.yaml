apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airqo-docs-website.fullname" . }}-test-connection"
  labels:
    {{- include "airqo-docs-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airqo-docs-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
