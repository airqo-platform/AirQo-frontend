apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airqo-packages.fullname" . }}-test-connection"
  labels:
    {{- include "airqo-packages.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airqo-packages.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
