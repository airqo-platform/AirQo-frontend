apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airqo-platform.fullname" . }}-test-connection"
  labels:
    {{- include "airqo-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airqo-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
