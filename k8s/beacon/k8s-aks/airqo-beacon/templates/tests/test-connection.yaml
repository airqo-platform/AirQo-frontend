apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airqo-beacon.fullname" . }}-test-connection"
  labels:
    {{- include "airqo-beacon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airqo-beacon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
