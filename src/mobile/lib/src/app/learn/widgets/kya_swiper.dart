import 'package:flutter/material.dart';

class KyaSwiper extends StatelessWidget {
  const KyaSwiper({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}