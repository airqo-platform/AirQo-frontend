/// When `true`, Exposure "My Places" shows the empty-state UI: persisted
/// declared places are not loaded and changes are not written to Hive.
/// Set to `false` for normal behavior (and to see real stored places again).
const bool kExposureDemoEmptyStateFirst = true;
