# Server-side only variables (not exposed to client)
API_URL=
OPENCAGE_API_KEY=
API_TOKEN=
SLACK_WEBHOOK_URL=
SLACK_CHANNEL=

# Client-side variables (these need NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# When running dev, Next will listen on the port we map (3000)
PORT=3000