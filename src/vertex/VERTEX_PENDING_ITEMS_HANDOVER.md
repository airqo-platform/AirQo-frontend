# Vertex Pending Items — Handover Review (Bwanika → Gibson)

Every item below was originally shared with Bwanika via Slack and re-checked against the current `airqo-frontend/src/vertex` code (and backend where relevant) to confirm it's still actually pending. Status legend: ✅ Resolved · 🟡 Partially done · 🔴 Still pending · ⚪ Not code-verifiable.

**Scope note:** Vertex is currently maintenance-only (no new features), now owned by Gibson. Items marked **[New feature]** below are net-new capability, not bug fixes — worth an explicit call on whether they're in scope before picking them up, vs. the **[Fix/UX]** items which are closer to normal maintenance work.

---

## ✅ Resolved — no action needed

| Item                                                                      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **"Add AirQo Device" label conflict**                                     | `claim-device-modal.tsx` now titled/labeled "Claim AirQo Device" everywhere it's used (home, my-devices, overview). Admin-only `create-device-modal.tsx` keeps "Add AirQo Device" and is confirmed reachable only from Admin → Networks. Claim modal also already has the suggested helper text ("Enter the device name and claim token from your shipment…") and a custom 404 message replacing the raw API text. |
| **Cohort "Assign Devices" toast bug (issue #3913)**                       | The success banner now reads the actual API response breakdown (`assigned` / `already_assigned`) via `getCohortAssignmentOutcome`, not the submitted request count. Falls back to request count only if an old backend deployment returns no breakdown at all.                                                                                                                                                     |
| **Bulk device import on "Assign Devices to Cohort" dialog (issue #3912)** | Resolved by integrating `DeviceNameParser` into `AssignCohortDevicesDialog` (`components/features/cohorts/assign-cohort-devices.tsx`). Users can now import devices from CSV/Excel spreadsheets with column mapping, name/label resolution, deduplication, and feedback banners, with full unit test coverage in `assign-cohort-devices.test.tsx`.                                 |
| **Networks API deprecation (Auth Service → "Sensor Manufacturers" page)** | Already migrated on 2026-09-03 (PR #3954, by Gibson) — `useNetworks()` now points at the device-registry-hosted networks endpoint, and the dead legacy permission-read path flagged as a silent-access risk was removed in the same PR. Nothing outstanding here.                                                                                                                   |

---

## 🟡 Partially done

| Item                                                      | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Historical measurements endpoint (device detail page)** | The endpoint vertex currently displays on the device detail "API access" card already matches the live, current backend route — no actual mismatch found today, so **Bwanika's suggestion to remove the historical endpoint isn't necessary**; it's valid and working. What's still missing: no link anywhere to the official partner docs page (`https://platform.airqo.net/docs/api/for-partners/historical-data/`) — worth adding as a "Learn more" link next to the existing copyable URL. **[Fix/UX]** |

---

## 🔴 Still pending

| Item                                                                         | Status                                                                                                                                                                                                                                                                                                                                           | Type        | Recommendation                                                                                                                                    |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Device decommission action** (issue #3924)                                 | Backend endpoint confirmed live and unchanged. Only "Recall Device" exists on the device details action bar — no "Decommission" action, and the `channelStatus`/`channelStatusCheckedAt` hint fields aren't surfaced anywhere on the page.                                                                                                       | New feature | **Accept** — mitigates real risk; without it, hardware engineers may reach for the destructive delete option instead.                             |
| **Self-service Cohort IDs (custom `cohort_slug`)**                           | Backend feature confirmed live (custom slug field + availability-check endpoint both exist and work). No UI for it yet — create-cohort form has no custom-ID field, and no availability-preview call exists.                                                                                                                                     | New feature | **Accept** — contained, backend already merged.                                                                                                   |
| **Shipping batch atomicity**                                                 | The "batch name" field in the prepare-shipping modal still goes through the older bulk-prep path instead of the dedicated atomic/rollback-safe one. Likely a small, contained swap per the original gap review.                                                                                                                                  | Fix/UX      | Keep as normal maintenance.                                                                                                                       |
| **Claim error-message mapping**                                              | Only the "device not found" case has custom copy. Already-claimed, token-mismatch, and expired-token cases still fall through to the raw backend message.                                                                                                                                                                                        | Fix/UX      | Keep as normal maintenance.                                                                                                                       |
| **Live device-name availability check in claim form**                        | The availability-check hook exists in the codebase but has zero call sites — not wired into the claim form UI.                                                                                                                                                                                                                                   | Fix/UX      | Keep as normal maintenance.                                                                                                                       |
| **Device transfer UI**                                                       | No UI at all — confirmed zero references anywhere in the codebase. Per the original gap review, this needs a product decision first (partner self-service vs. staff-only tool) before building anything.                                                                                                                                         | New feature | **Defer** — pure growth investment, no safety/support-load case; needs a product check-in before any build decision.                              |
| **"Sign in with a different account" (multi-account switching)**             | Still single-account-only. A dormant "recently used accounts" list exists in local storage but stores no tokens, isn't wired into any UI, and doesn't let a second account stay signed in alongside the first. Spans Nexus and Beacon too per the original note — worth a shared design pass across all three rather than three separate builds. | New feature | **Defer** — cross-app scope, no urgency signal; check appetite before committing.                                                                 |
| **Partner network leaderboard / verified directory / per-owner impact view** | All three backend endpoints confirmed live (leaderboard, public directory, my-impact). Zero frontend UI for any of it — no directory page, no leaderboard widget, no impact card. Explicitly optional/no-urgency in the original note.                                                                                                           | New feature | **Defer** — explicitly no-urgency in the original note; may tie to a specific incentive-program push, worth confirming before declining outright. |

---

## ⚪ Not code-verifiable (process items, not technical debt)

- **Monthly sprint goal + sprint-planning doc refresh** — this was a chat/planning ask, not something confirmable against the codebase. Still outstanding as far as this review can tell; worth a direct check-in.
- **`#notifs-vertex-web` error log triage** — an ongoing operational ask rather than a fixed code change; recommend a fresh pass now that ownership has moved to Gibson.

---

## Suggested next step

Given Vertex's maintenance-only status, triage on risk/cost rather than feature-vs-fix alone:

- **Do as normal maintenance:** all **Fix/UX** items, plus the two **Accept**-tagged new-feature items above (decommission action and self-service cohort ID) — each is either risk-reducing or small and mostly backend-complete already.
- **Defer, pending a product check-in:** device transfer, multi-account switching, and the partner leaderboard/directory/impact UI. None have an urgency signal in the original notes, but declining them outright in this doc risks silently dropping something with product backing (e.g. the leaderboard may tie to an incentive-program push) — worth a one-line confirmation with whoever owns that call before closing them out.
