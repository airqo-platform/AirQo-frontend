/* eslint-disable */
const withVideos = require('next-videos');

module.exports = withVideos({
  transpilePackages: ['redux-persist'],
  experimental: {
    // Suppress Edge Runtime warnings for specific modules
    serverComponentsExternalPackages: ['axios'],
  },
  // Suppress specific webpack warnings
  webpack(config, { isServer }) {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'flagsapi.com',
      },
      {
        protocol: 'https',
        hostname: 'asset.cloudinary.com',
      },
      {
        protocol: 'https',
        hostname: 'res.cloudinary.com',
      },
      {
        protocol: 'https',
        hostname: 'www.vhv.rs',
      },
      {
        protocol: 'https',
        hostname: 'img.freepik.com',
      },
    ],
  },
  reactStrictMode: false, // Temporarily disabled to fix "Should have a queue" error
  eslint: {
    dirs: ['pages', 'components', 'lib', 'utils', 'hooks'],
  },  webpack(config) {
    config.module.rules.push({
      test: /\.svg$/,
      use: ['@svgr/webpack'],
    });

    config.resolve.fallback = {
      ...config.resolve.fallback,
      fs: false,
      net: false,
      tls: false,
    };

    // Exclude problematic Node.js modules from Edge Runtime
    if (config.name === 'edge-runtime') {
      config.resolve.alias = {
        ...config.resolve.alias,
        axios: false,
        // Also exclude the logger that imports axios
        '@/lib/logger': false,
      };
    }

    // Add externals for server-side bundles to reduce warnings
    if (config.target === 'node') {
      config.externals = config.externals || [];
      config.externals.push({
        'axios': 'axios',
      });
    }

    return config;
  },

  async rewrites() {
    return {
      beforeFiles: [
        // Use API routes for dynamic generation
        {
          source: '/robots.txt',
          destination: '/api/robots',
        },
        {
          source: '/sitemap.xml',
          destination: '/api/sitemap',
        },
      ],
    };
  },
});
